{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Butly shell の main window に与える最小権限。shell/fs 系は一切許可しない（sidecar 起動は Rust 側のみ）。",
  "windows": ["main"],
  "permissions": ["core:default"]
}
