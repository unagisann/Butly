# Gatekeeper 入出力仕様・プロンプト注入まとめ

現在の `butly_core/core/gatekeeper.py` などの実装に基づく、Gatekeeper（前頭葉）モジュールでの入出力データと、判定された各Tier（階層）に応じてメインのAI（Brain）へ最終的に渡される情報の一覧です。

---

## 1. Gatekeeper が【受け取る情報】（判定時の入力）

Gatekeeperが「ユーザーの発言の複雑さ・必要な記憶レベル」を判断するために、軽量なLLM（Gemini）へ渡しているコンテキストです。

- **ユーザーの最新の発言** (`user_input`)
- **直近の会話履歴** (`history_msgs`): 直近のやり取り（最大3ターン分）。
- **セッション状態** (`session_state`): セッションごとの動的な状態情報。
  - `Topic`: 現在の話題
  - `Mood`: ユーザーや会話のムード
  - `Goals`: 現在の目標リスト（最大5件）
  - `Unresolved`: 未解決事項のリスト（最大8件）
  - `Turn`: セッションの経過ターン数

---

## 2. Gatekeeper の【出力】（判定結果）

軽量LLMによる判定後の出力（内部状態の更新用データ）です。

- **`tier`**: 判定された階層（`reflex`, `mid`, `cortex` のいずれか）
- **`state_delta`**: セッション状態（GoalsやUnresolved事項など）を追加・解決・更新するための差分データ。
- **`need` / `search_targets`**: `cortex`（大脳皮質）と判定された場合にのみ生成される「追加で必要な情報」と「検索対象キーワード」。

---

## 3. 各Tier判定後にメインAIへ【渡される情報（注入されるプロンプト）】

Gatekeeperの判定結果（Tier）を受け取り、`MemoryBlockBuilder` がシステムプロンプト（System Instruction）を組み立ててメインのAI（応答生成用LLM）に渡します。

### 🔘 全Tier共通で【必ず渡される情報】

どのTierに判定されても、これらは常にAIの前提として渡されます。
1. **SYSTEM INSTRUCTION**: AIの基本性格・システム設定。
2. **KEY MEMORY**: 主人（ユーザー）に関する根幹・不変の記憶。
3. **CURRENT TIME**: 現在時刻（システムノート）。
4. **直近の会話履歴 (Short Term)**: 通常直近6ターンの実際のメッセージ履歴。
5. **FLOATING SUMMARY (未整理記憶)**: 最新の対話の流れをまとめた浮動要約。
6. **TIER INFO**: 現在AIがどの階層（reflex/mid/cortexのどれか）で思考するべきかの明示。

---

### 🔵 Tier別に追加される情報

#### 【 Tier 1 】 reflex（脊髄反射）
- **追加される情報**: **なし**
- **特徴**:
  - 「共通で渡される情報（直近のやり取りやショートターム要約）」のみで即座に返事をします。
  - 挨拶、相槌、「うんわかった」といった、知識検索を待たずにテンポ良く返すべき内容で発動します。

#### 【 Tier 2 】 mid（中脳・感情系）
- **追加される情報**: 以下のいずれか（設定による）
  - **MID-TERM MEMORY**: 中期記憶のテキスト全体（RAWデータ）
  - もしくは **MID-TERM DIGEST (事実ダイジェスト) ＋ RELATIONSHIP SNAPSHOT (関係性)** （設定が要約モードの場合）
- **特徴**:
  - 現在の話題に関する具体的な質問や、少し前のやり取りの前提を踏まえる必要がある会話で発動します。
  - セッション内の文脈や事実関係は網羅しますが、過去の長期記憶まで掘り起こす必要はない段階です。

#### 【 Tier 3 】 cortex（大脳皮質）
- **追加される情報**:
  - **mid の情報すべて**（MID-TERM MEMORY等）
  - ➕ **LONG-TERM MEMORY (RAG)**: データベース（`butly_memory.db`）からの検索結果
- **特徴**:
  - 「あの時の」といった過去への言及や、専門的・技術的な深い考察が必要な問いで発動します。
  - Gatekeeperが抽出したキーワードを元に過去の記憶からRAG検索を行い、ヒットした関連情報（エピソードなど）をプロンプトに追加して回答します。
