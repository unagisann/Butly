# RAW 注入の見直し計画 — 「根拠は残っているのに配線されていない」

**ステータス**: v40・Oracle Reader v41 ともに完了（Semantic Judge 含む、2026-09-02）
**残作業**: provenance 救済（寄与41%）→ 注入枠・ランキング（32%）→ 段階別失敗分類の成果物化 → AMemGym 接続

この文書は 2026-08-23 の調査で分かったことを、次の判断のためにまとめたものである。
数値はすべて `eval_runs/runs/` の成果物から算出した実測で、推定には「概算」と明記する。
検索・想起まわりの評価履歴は [RAG評価・改善レポート](../../history/rag_evaluation_report.ja.md) が正本。

---

## 1. きっかけ: 評価が目的化していた

直近の作業は測定器の整備に偏っていた（公式互換 Token F1 の系統誤差、Semantic Judge の
経路差、反転率、ノイズ床）。必要ではあるが、検索も想起も改善していない。
そこで「検索・想起の実課題は何か」を数え直したところ、**前提が間違っていた**ことが分かった。

## 2. 失敗の内訳を取り直した

`analyze-evidence` を現行 corpus（1,105カード・全1,986問）で回した結果。
これまで参照していた内訳は v31（conv-26 の199問・qwen 製107カード）のもので、**順位が逆だった**。

| 落ちた場所 | v37 (注入3枚) | v39 (注入10枚) | 効く打ち手 |
|---|---:|---:|---|
| **根拠を含むカードが無い** | **210（42.2%）** | **210（56.6%）** | カード化 / provenance |
| 候補 top20 に入らない | 115（23.1%） | 115（31.0%） | embedding / 検索クエリ |
| 候補には居るが注入枠の外 | 173（34.7%） | 46（12.4%） | ランキング / 注入枠 |
| 検索が走っていない | 0 | 0 | — |
| **evidence=0 合計** | **498** | **371** | |

母数は cat5 を除く 1,536問。

### 2.1 ここで一番重い事実

top-3 → top-10 で **127問に新しく根拠が届いた**（evidence=0 が 498→371）。
それなのに**公式互換は +0.0088、cat1-4 でも +0.0152 しか動かなかった**。

さらに、**根拠がすでに届いている994問のうち189問（19%）は0点**である。
正解の発話がプロンプトに入っていても2割は外す。

> **根拠を届けることと、答えられることは別。** 検索側の改善は、この換算率で割り引いて
> 期待値を置く必要がある。

**この割り引き方は後に v41 Oracle Reader で修正した。§7.2.2 を参照。** 換算率は
「根拠が届いたか」の二値ではなく**完全性**で決まる（全件届いた層42.5% / 一部だけ27.4%）。
また「根拠がすでに届いている994問のうち189問が0点」も、実注入ベースで数え直すと
**原文が全件届いた833問のSemanticは0.859**であり、この層に伸びしろはほぼ無い（§7.2.1）。

## 3. `no_card` 210問の正体

「カードに事実が無い」と分類された問題を、archive の原文と突き合わせた。

| | 問数 |
|---|---:|
| `no_card` 判定（cat5 込み） | 279 |
| ├ うち official ≥ 0.5（実際は答えている） | 92（33%） |
| evidence の発話が archive に残っている | **272 / 279（97.5%）** |
| ├ **注入されたカードと同じ日の RAW に在る** | **178** |
| └ その日のカードが注入されていない | 94 |
| **archive にも無い＝本当の欠落** | **7** |

`no_card` は「gold evidence のファイルと `source_files` が重なるカードが無い」という
**ファイル粒度の近似**であって、事実がどこにも無いという意味ではない。
RAW 保存層まで含めて回収不能なのは7問だった。一方、カード本文から落ちた事実の量は
別途意味的に測る必要があり、この7問をそのまま「カード化による欠落」とは扱わない。

実際に8問読んだ結果も同じ方向だった。`conv-30_qa_043` は「regionals で1位を取った」は
カードに残り、演目名 "Finding Freedom" だけが要約で落ちている。しかしその発話は
`session_20230120_160400_000000_009.json` に原文のまま残っている。

## 4. RAW がどう配線されているか

```
knowledge_cards.source_files  →  発話単位の archive ファイル（1〜2個/カード）
knowledge_cards.raw_reference →  "2023-01-20_raw_combined"（日単位のラベル・未使用）
                ↓
resolve_raw_reference(candidates, top_k=rag_raw_top_k)
                ↓
  カードのスコア順に source_files を集め、文字数予算に入るだけ greedy
```

- **`rag_raw_top_k` の既定は 1**。つまり**最上位カード1枚ぶんの原文しか展開していない**。
- 実測: 注入 RAW は平均 **1.94ファイル / 671文字**。`rag_raw_max_chars` は **2,500**。
  **予算の 27% しか使っていない。**
- **RAW 本文の関連度は一度も見ていない。** 並び順はカードのスコア順、表示は時系列順。

### 4.1 passage リランクは「計算済みで捨てている」

`EvidenceFusion.rerank()` はカードごとに Episode と **RAW チャンク（1,800字刻み）** を
ユニット化し、全ユニットを埋め込んで質問との cosine を取り、**MaxP の勝者**を選んでいる。
勝ったユニットの `evidence_type` と **`source_file` は `evidence_fusion_scores` に記録済み**。

**この勝者情報を注入側は使っていない。** カードの順位付けに使って捨て、RAW は別ロジック
（カード順）で選び直している。

## 5. 「同じ量で入れ替える」は負ける

`evidence_fusion_scores` を使って、注入量を変えずに RAW を passage スコア順へ
置き換えた場合を模擬した（1,974問）。

| | evidence 命中 | 注入ファイル数 |
|---|---:|---:|
| 現行（1位カードの原文を全部） | 994（50.4%） | 1.94 |
| passage スコア順に同数だけ | **792（40.1%）** | 1.93 |

内訳は維持 631 / 取りこぼし 363 / 新規 161 = **差し引き -202問**。

**理由**: 現行は1位カードの `source_files` を*連続した発話ファイルごと*渡している。
正解は「質問に最も似た1チャンク」ではなく**その隣の発話**にあることが多い。
`conv-30` の例（カードは `_008`、正解は `_009`）がそれで、passage 単位で最良を1つ選ぶと
近傍が切れる。**会話ログでは「最も似た断片」より「近傍ごと」のほうが当たる。**
通常の passage retrieval の定石と逆であり、この点は記録しておく価値がある。

## 6. 効くのは「足す」方向、しかも予算内

> **注記（2026-09-02）**: 本節と§4〜§5の「渡し方をもっと良くする」という前提は、
> v41 Oracle Reader により**否定された**。原文が全件届いた層ではRAW3がoracleを上回る。
> §7.2.1 を参照。RAW1→RAW3の「足す」判断そのものは有効で、覆っているのは
> 「さらに良い渡し方が残っている」という見込みのほうである。

現行を保ったまま、passage スコア順に別カードの原文を足した場合。

| RAW 展開 | evidence 命中 | 新規カバー | うち現状0点 | RAW 文字数 |
|---|---:|---:|---:|---:|
| 1（現行） | 994（50.4%） | — | — | 587 |
| 2 | 1,225（62.1%） | 180 | 55 | 1,140 |
| **3** | **1,316（66.7%）** | **252** | **86** | **1,652** |
| 5 | 1,420（71.9%） | 327 | 116 | 2,338 |

この表は事前見積もりである。実走では RAW3 の平均文字数は1,835文字だったが、
**295 / 1,986問（14.9%）が `rag_raw_max_chars: 2500` で切られた**。ファイル区切りなどの
実際のprompt構築コストを、平均値だけでは判定できなかった。

### 6.1 注入カード枚数は evidence 命中に効かない

| カード | RAW 展開 | evidence 命中 |
|---:|---:|---:|
| 3 | 1 | 994（50.4%） |
| 3 | 3 | **1,316（66.7%）** |
| 10 | 1 | 994（50.4%） |
| 10 | 3 | 1,316（66.7%） |

fusion の順位は同一なので上位3枚は常に同じ3枚であり、**カードを増やしても RAW 命中は
1ミリも動かない**。「カード3枚＋RAW3」は「カード10枚＋RAW1」より **+322問**、かつ
カードは7枚少ない。

概算トークン（v37 1,822 / v39 2,431 から逆算してカード1枚 ≒ 87 token、
RAW 1→3 は +1,065文字 ≒ +270 token）:

| 構成 | prompt 概算 | evidence 命中 |
|---|---:|---:|
| v37 カード3・RAW1 | 1,822 | 50.4% |
| v38 カード5・RAW1 | 1,996 | 50.4% |
| v39 カード10・RAW1 | 2,431 | 50.4% |
| **v40 カード3・RAW3** | **約2,090** | **66.7%** |

## 7. v40 の結果

`gpt-4o-mini_web_v40_top3_full_Raw3`（1,986問、完了）。

- v37 の profile との差分は **`rag_raw_top_k: 1 → 3` の1行のみ**（＋judge ブロック）
- 記憶は v37 を再利用。`card_identity.json` は v38/v39 とバイト一致、dataset hash も一致、
  Stage 3 bootstrap は `reviewed_cards: 0 / llm_calls: 0` の no-op
- Judge は `google/gemma-4-31b-it` / `nanogpt-sub` / temp 0 / max_output 4096

検索指標は v37 と完全に同一で、差は `rag_raw_top_k: 1 → 3` による原文注入だけである。

| 指標 | v37 RAW1 | v40 RAW3 | 差 | t |
|---|---:|---:|---:|---:|
| 公式互換 overall | 0.4646 | **0.4828** | +0.0182 | +3.89 |
| **cat1-4** | 0.4225 | **0.4524** | **+0.0299** | **+6.44** |
| cat5 | 0.6099 | 0.5874 | -0.0224 | -1.72 |
| prompt tokens平均 | 1,822 | 2,127 | +16.7% | — |

新しくgold RAWが届いた308問では公式互換が+0.0740（t=3.51）、改善84 / 悪化31で、
**換算率は84 / 308 = 27.3%**だった。この層だけで全体差+0.0182の63%を説明する。
元から根拠が届いていた994問は+0.0055（t=0.97）なので、主効果は期待した機構に集中した。

一方、cat5では「情報がない」と答えた数が272→262、contradictionが168→177となった。
有意差ではないが、追加RAWから答えを推測して誤る方向は明確である。RAW3はLoCoMo上の
比較条件として有用だが、これだけで本番既定へ昇格させない。

Semanticは0.6118。ただし v37 の保存値は旧Judge経路なので直接比較しない。

### 7.1 Oracle Reader v41 — 読解の上限と、換算率の正体

`gpt-4o-mini_web_v41_oracle_reader`（1,986問、Semantic Judgeまで完了）。
`rerun-qa --qa-memory-mode oracle`でv40の記憶を複製し、検索・カード・RAW bundle・
中期記憶・active nodeをすべて止めて、各問の`question.evidence`が指す原文ターンだけを
注入した。gold answerは渡していない。**「検索が完璧だったら読解はどこまで行くか」**の上限である。

条件の同一性はrun成果物で確認した。`card_identity.json`の10 conv全てで`digest`が一致
（1,105カード）、`dataset_manifest.json`はバイト一致、profileはv40と同一ファイル。
Judge経路も同じ（`config_signature: 4e802e9d…`）。差はQA時の注入内容だけである。
注入は平均1.42ターン / 390文字で、解決失敗は根拠0件4問（すべてcat3）、
dialog ID欠落2問、注記の不正1問だった。

| 指標 | v40 RAW3 | v41 Oracle | 差 | t |
|---|---:|---:|---:|---:|
| 公式互換 overall | 0.4828 | **0.5925** | +0.1097 | **+10.31** |
| **cat1-4** | 0.4524 | **0.6258** | **+0.1733** | **+16.84** |
| cat5 | 0.5874 | 0.4776 | -0.1099 | **-3.78** |
| Semantic cat1-4 | 0.6175 | **0.7753** | +0.1578 | **+12.48** |
| prompt tokens平均 | 2,127 | 681 | -68.0% | — |

### 7.2 伸びしろを段階別に割り当てる

v41があるので、各段階を完全に直したときの回答上限を実測で当てられる。
cat1-4の1,531問（provenanceを解決できた問）を、**gold evidenceの原文が実際にpromptへ
入ったか**で排他分類する。判定は段階1〜3が`analyze-evidence`と同じ指標
（`search_executed` / `oracle_available` / `recall_at_20` / `recall_at_3`）、
段階4〜6が`diagnostics.rag.raw_reference.files`とgold evidenceファイルの突き合わせである。

| 落ちた段階 | 問数 | 割合 | v40 | 上限 | 差 | 寄与 | 寄与率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **1 カードが根拠を指していない** | 205 | 13.4% | 0.302 | 0.785 | +0.483 | +0.0647 | **41%** |
| 2 候補top20に入らない（検索） | 115 | 7.5% | 0.283 | 0.687 | +0.404 | +0.0304 | 19% |
| **3 top3に来ない（ランキング）** | 173 | 11.3% | 0.249 | 0.699 | +0.451 | +0.0509 | **32%** |
| 4 top3なのにRAW展開されず | 9 | 0.6% | 0.278 | 1.000 | +0.722 | +0.0042 | 3% |
| 5 一部しか届かない（粒度） | 196 | 12.8% | 0.462 | 0.643 | +0.181 | +0.0232 | 15% |
| 6 全件届いた（利用） | 833 | 54.4% | **0.859** | 0.832 | **-0.027** | -0.0147 | -9% |

**§1〜§3で92%を占める。** どれも「原文はどこかにあるのに、正しいものを引けていない」で、
カード本文の圧縮品質でも、readerの能力でもない。

### 7.2.1 §4〜§6を見直す: 渡し方は課題ではなかった

§4〜§6は「RAWの渡し方（`rag_raw_top_k`、passageスコア順、近傍bundle）」を改善余地として
扱ってきた。**v41はこれを否定する。**

**原文が全件届いた833問では、v40の0.859がv41の0.832を上回る。**
RAW3がファイル粒度で近傍ごと渡す「余分な発話」はノイズではなく**利益**であり、
goldターンだけを純粋に渡すoracleのほうが負けている。§5で「passage単体へ置き換えると
-202問」と観測したのと同じ現象が、coverageではなく**回答スコア**でも再現した。

したがって**近傍bundleの優先度は下げる**。RAW3は既にその効果を得ており、
「もっと良い渡し方」で回収できる余地は段階4（9問・寄与3%）しか残っていない。

渡し方で実在するのは段階5「一部しか届かない」196問（寄与15%）だけで、これは中身の質ではなく
**量**の問題である。この層は154/196がcat1（multi-hop）で必要evidenceが平均3.50ターン、
段階6の1.12ターンと対照的である。打ち手は粒度の工夫ではなく、
**multi-hop問で必要ターンを揃えきること**になる。

### 7.2.2 §2.1 を修正する: 換算率は完全性で決まる

§2.1では「根拠を届けることと答えられることは別。検索側の改善はこの換算率で割り引いて
期待値を置く必要がある」と書いた。**割り引き方が間違っていた。**

v37→v40で原文が新しく届いた215問を、届いた完全性で割る（公式互換ベース）。

| | 問数 | 改善 | 換算率 |
|---|---:|---:|---:|
| **全件届いた** | 153 | 65 | **42.5%** |
| 一部だけ届いた | 62 | 17 | 27.4% |

**「根拠が届いた」を二値で数えると換算率を過小評価する。** §7の27.3%は
`evidence_coverage`（カードの`source_files`基準で、部分被覆を含む）で数えた値である。
実注入ベースで全件揃った層に限れば42.5%になる。

> **今後「新規カバー→回答改善の換算率」を出すときは、部分被覆と全件被覆を必ず分ける。**
> 二値のcoverageで割った換算率は、施策の効果を過小評価する方向に偏る。

なおv41のevidence=0層の換算率（63.9%）とこの42.5%を直接並べてはいけない。前者は
「v40が届けられなかった502問」、後者は「RAW3が届けられた215問」で母集団が排他であり、
差を届け方の優劣へ帰属できない。**比較できるのは同一母集団の段階別上限（§7.2の表）だけである。**

### 7.3 `utilization_miss` の床は17%

**gold根拠を直接渡してもcat1-4は0.6258（Semantic 0.7753）で止まる。**
1,540問のうち262問（17.0%）はSemanticで`incorrect`のままで、これは§9の分類でいう
`utilization_miss`を、検索・カード化・注入の影響を完全に除いて測った値である。

したがってこの計画（write / retrieval / injection の改善）で取れる上限は
**cat1-4 で +0.17** である。それを超える改善はreader側（プロンプト、モデル、回答形式）に
属し、本計画の対象外として分離する。

### 7.4 cat5 の低下はLoCoMo注記の性質であって施策の副作用ではない

| cat5（446問） | v40 RAW3 | v41 Oracle |
|---|---:|---:|
| 「情報なし」と答えた | 262（58.7%） | 213（47.8%） |
| contradiction | 177 | 230 |

LoCoMoは「No information available」が正解の問にもevidenceターンを付けており、
その指し先は正解に似た**別の**発話である。確実に届けるほどモデルは棄権をやめる。
例: `What country is Melanie's grandma from?`（正解: No information available）は
v40が正しく棄権し、v41は注入された`D4:3`を根拠に`Sweden.`と答えた。

**cat5はRAW注入施策の副作用監視指標として引き続き使うが、oracle条件のcat5低下を
施策の欠陥として読んではいけない。** 読解上限としてはcat1-4を使う。

## 8. 評価の役割分担

この計画で LoCoMo を使う目的は、本番会話の品質や設定既定を決めることではない。
固定済み会話を同一条件で replay し、記憶パイプラインを段階別に検証することである。

| 評価レイヤー | 主に測るもの | この計画での扱い |
|---|---|---|
| **LoCoMo** | 固定会話からのナレッジ化、検索、注入、回答での利用 | 現在の対象 |
| **AMemGym** | 対話へ参加しながら行う記憶の書き込み・更新・検索・利用 | LoCoMo の次段 |
| **実ユーザー評価** | 現実の会話分布での有用性、安全性、過剰想起 | 別計画・本書の対象外 |

LoCoMo では次の連鎖を一つずつ測る。

```
固定会話 → RAW保存 / カード生成 → 候補検索 → prompt注入 → 回答利用
```

ここでの「ナレッジ化」は、全事実をカード本文へ複製することだけを指さない。
**カードを検索インデックスとして生成し、provenance から必要な原文へ戻れる状態を作ること**も
含む。したがって RAW 注入はカード化の失敗時だけの救済ではなく、カードと原文を役割分担
させる記憶方式として評価する。

AMemGym は、固定履歴を読む LoCoMo では測れない on-policy の対話的評価へ使う。
アシスタント自身の応答を含む会話軌跡の中で、状態変化・訂正・古い記憶との競合を扱えるかを
検証する。AMemGym も構造化状態を持つシミュレートユーザーによる評価であり、実ユーザーでの
外的妥当性はさらに後段である。

- [AMemGym 公式サイト](https://agi-eval-official.github.io/amemgym/)
- [AMemGym ICLR 2026 論文](https://openreview.net/forum?id=sfrVLzsmlf)

本番の `rag_source_mode`、`rag_raw_top_k` の既定変更や既存実インスタンスの移行は、
LoCoMo 上の記憶パイプライン評価とは分離し、この計画の採否条件に入れない。

## 9. LoCoMo 内での失敗分類

現行の `no_card` / `evidence=0` だけでは原因を一意に表せない。今後は少なくとも次へ分ける。

| 分類 | 意味 |
|---|---|
| `raw_missing` | gold evidence が保存済み RAW に無い |
| `card_content_missing` | RAW にはあるがカード本文に事実が残っていない |
| `provenance_missing` | RAW はあるがカードから該当ファイルへ辿れない |
| `retrieval_miss` | 根拠カードが候補 pool に入らない |
| `selection_miss` | 候補にはあるが実際の prompt に根拠が入らない |
| `utilization_miss` | 根拠が prompt に入っているのに回答できない |

主指標は全体スコアだけでなく、RAW1→RAW3で新しく根拠が届いた質問に限定した換算率とする。

```
新規カバーから回答改善への換算率
  = RAW3で改善した質問数 / RAW3で新しく根拠が届いた質問数
```

既に根拠が届いていた質問の改善 / 悪化、`utilization_miss` 率、cat5 の誤利用、prompt tokensも
併記する。cat5 はナレッジ化の主指標ではなく、RAW追加による副作用の監視指標として扱う。

## 10. 今後の方針

優先順は§7.2の寄与率で決める。**打ち手は「原文へ正しく辿り着く」側に寄せ、
「渡し方を良くする」側からは外す。**

1. ~~**v40を完走し、v37と同一Judge経路で比較する。**~~ 完了（§7）。
2. ~~**読解上限を測る。**~~ 完了（§7.1〜7.4）。cat1-4 0.6258、`utilization_miss`の床17.0%、
   段階別の寄与率を得た。
3. **provenance救済を評価する（最優先・寄与41%）。** 段階1「カードが根拠を指していない」
   205問が単独で最大である。事実が消えているのではなく（§3: archiveに272/279が残存、
   真の欠落は7問）、`source_files`からgold evidenceのファイルへ辿れない。
   `source_files`欠落時に`raw_reference`の日付範囲から候補を復元できるかを検証し、
   正確な生成時provenanceと後付け推定を区別して記録する。
   **判定基準は段階1の問数と、その層のSemanticが上限0.785へどこまで寄るか。**
4. **注入枠とランキングを見直す（寄与32%）。** 段階3「候補には居るのにtop3へ来ない」173問。
   候補には入っているので検索は当たっており、落ちているのは順位付けと注入枠である。
   [RAG評価・改善レポート](../../history/rag_evaluation_report.ja.md)§5.10でtop-10を不採用にした判断は維持しつつ（promptが+21.8%増えるため）、
   **枠を増やさずに順位を直す**方向——リランカー、Fusion重み、`rag_raw_top_k`との
   組み合わせ——で測る。
5. **multi-hopの必要ターンを揃える（寄与15%）。** 段階5「一部しか届かない」196問は
   154問がcat1で、必要evidenceが平均3.50ターンある。粒度の工夫ではなく、
   **1問に必要な複数ターンを揃えきる**問題として扱う。
6. **候補pool側の検索を改善する（寄与19%）。** 段階2「top20に入らない」115問。
   embedding・検索クエリの領域である。[RAG評価・改善レポート](../../history/rag_evaluation_report.ja.md)§6.5・§6.6に
   候補pool 20→50がRecall@3で最大+1問だった記録があるため、pool拡大ではなくクエリ側を疑う。
7. **段階別の失敗分類を成果物へ追加する。** §7.2の分類を`analyze-evidence`へ組み込み、
   実注入ベース（`raw_reference.files`との突き合わせ）まで含めた
   question-level診断を毎runの成果物にする。`utilization_miss`はv41の
   question-level結果を正解ラベルとして使えば通常runでも直接分離できる。
8. ~~**近傍bundleを比較する。**~~ **優先度を下げる（§7.2.1）。** 原文が全件届いた833問では
   RAW3（0.859）がoracle（0.832）を上回り、近傍を混ぜること自体が既に効いている。
   「もっと良い渡し方」で回収できる余地は段階4の9問（寄与3%）しかない。
9. **RAW3をLoCoMo上の評価基準として判断する。** 本番既定ではなく、同じナレッジから
   どれだけ根拠を呼び出して利用できるかの比較条件として決める。
10. **LoCoMoの段階評価が安定した後、AMemGymへ接続する。** 同じwrite / read / utilization診断を
    対話中の記憶更新、訂正、状態変化へ拡張する。

**この計画の上限は cat1-4 で +0.17 である**（§7.3）。それを超える改善はreader側に属し、
本計画の対象外として分離する。3〜7でその+0.17のうちどれだけ取れたかを進捗の尺度にする。

カード生成粒度の変更は、`card_content_missing` の実測後に判断する。段階1の上限が0.785と
高いことは「原文へ辿り着けば答えられる」を意味するが、カード本文の圧縮品質そのものを
測ったわけではないため、現時点では優先度を確定しない。

---

## 参照

- [RAG評価・改善レポート](../../history/rag_evaluation_report.ja.md) — 評価履歴の正本（§5.10 top-k 曲線、§5.11 Judge 経路、§5.12 Oracle Reader）
- [LoCoMo評価フロー](../../reference/locomo_evaluation_flow.ja.md) — `rerun-qa` と記憶再利用
- [`raw_reference.py`](../../../butly_core/core/gatekeeper/raw_reference.py) — RAW 解決の本体
- [`evidence_fusion.py`](../../../butly_core/core/evidence_fusion.py) — passage スコアの計算箇所
- [`defaults.py`](../../../butly_core/settings/defaults.py) — `rag_source_mode` / `rag_raw_top_k` / `rag_raw_max_chars`
