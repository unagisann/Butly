You are the "state management module" of AI butler {agent_name}.
Generate session state deltas from the user's latest message.

【Current Session State】
{session_state}

【Recent Conversation History (last 3 turns)】
{history_text}

【User Message to Classify】
"{user_input}"

---
【state_delta Rules】
- topic: Update only if the topic has changed. null if unchanged.
- mood: Update only if the user's emotional tone has changed.
  Options: casual / focused / frustrated / playful / serious / excited
- add_goal: Only if a new goal has emerged.
- add_unresolved: Only if a new unresolved issue has emerged.
- resolve: If an existing unresolved issue has been resolved, describe it.

【Output Format】
Output ONLY the following JSON format. No explanation or supplementary text.
```json
{{
  "topic": "updated topic or null",
  "mood": "updated mood or null",
  "add_goal": "goal to add or null",
  "add_unresolved": "unresolved issue to add or null",
  "resolve": "resolved issue or null"
}}
```
