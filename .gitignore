# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual Environment
venv/
ENV/

# Environment variables (Sensitive)
.env
APIkey.env
*.env
# Vite のローカル env（frontend/.env.development.local 等）。*.example は対象外。
.env.*.local

# Database
*.db
llm_capabilities.json
!butly_memory.db.example # 例として残す場合は除外

# AI agent local rules (not published)
AGENTS.md
CLAUDE.md

# Local config
user_config.json
user_prompts.json
system_config.json
external_accounts.json
persons.json

#Dashbord用
discovery_agent.py
discovery_cache.json
news_agent.py
news_cache.json

# Instance data (Personal/Private history)
butly_core/instances/

# OS
.DS_Store
Thumbs.db

# Misc
housekeeper.log
*.bak
build_pyinstaller/
# ルート直下の作業用 old/ だけを無視する。docs/Old/（凍結ドキュメント）は追跡する
# — Windows は core.ignorecase=true なので、無印 `old/` だと `docs/Old/` まで巻き込む。
/old/
!/docs/Old/
old_migration_backup/
server.spec
analysis.txt
flake8_critical.txt

# Local evaluation artifacts
eval_runs/
/docs/temp/
/data/locomo*.json
